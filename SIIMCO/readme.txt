1. In this fold:
    
   SIIMCO.png is shown as the formulas we implement. The symbols in the formula are also introduced in the Fig..
   
   total_statistics.csv is the statistics about the number of emails sent by each employee to others. The first row and the first column are the employee ID. This file is getted by caculating the emails of the pre-processing data.

   SIIMCO.py contains the codes that implements the formula in Fig. SIIMCO.png and F1-score, recall, precision are output by the code.
